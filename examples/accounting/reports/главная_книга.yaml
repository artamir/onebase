name: ГлавнаяКнига
title: Главная книга
params:
  - name: Счёт
    type: string
    label: "Счёт"
  - name: Начало
    type: date
    label: "С даты"
  - name: Конец
    type: date
    label: "По дату"
query: |
  ВЫБРАТЬ
    period КАК Период,
    регистратор_тип КАК Документ,
    СчётДт,
    СчётКт,
    Сумма
  ИЗ РегистрБухгалтерии.БухУчёт
  ГДЕ (&Счёт ЕСТЬ ПУСТО ИЛИ СчётДт = &Счёт ИЛИ СчётКт = &Счёт)
    И period >= &Начало
    И period <= &Конец
  УПОРЯДОЧИТЬ ПО period
