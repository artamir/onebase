# OneBase: неделя после релиза v0.3.27 (22–29 мая 2026)

> Сводка по итогам недели: 94 коммита (90 без merge-коммитов), +35 000 / −2 257 строк, 229 файлов, 4 влитых PR (#9, #10, #11, #15), закрыты issue #8 и #13.

---

## 1. Что сделано за неделю (список изменений)

### 🧩 Управляемые формы (План 37 + План 40) — главная фича недели
Полноценный объект метаданных «управляемая форма»: декларативный YAML рядом с DSL-модулем, UI-редактор (split-pane Monaco + live preview) и двусторонний конвертер с форматом управляемых форм Enterprise-системы.

- **feat** Foundation управляемых форм и конвертера (этап 1)
- **feat** Импорт управляемых форм из 1С (этап 2)
- **feat** Рантайм форм + опциональный выбор (этап 3)
- **feat** UI-редактор управляемых форм в конфигураторе (этап 4)
- **feat** Экспорт управляемых форм OneBase → 1С (этап 5)
- **docs** Validate CLI + спецификация + руководство (этап 6)
- **feat** Блок «◇ Управляемая форма» на странице сущности
- **feat** Рантайм событий управляемых форм (этап 8)
- **feat** Объект-обёртка, `ТЧ.Добавить`, `ПриОткрытииФормы`, ISO-даты
- **feat** Движения регистров в managed-форме
- **feat** Управляемые формы для обработок (План 40)
- **fix** Ссылки в ТЧ управляемой формы как select, а не UUID
- **fix** Сериализация ссылок в ТЧ managed-формы + единый стиль
- **fix** UX управляемой формы: активная вкладка, кнопки ТЧ и «+»
- **fix** Корректное экранирование YAML/OS в Monaco-редакторе

### 🌍 Интернационализация (PR #10)
Локализация интерфейса и многоязычные синонимы метаданных.

- **feat** Локализация UI: немецкий, сербский + исправление резолва языка
- **feat** 12 новых языков интерфейса — KA HY ES KK AZ UZ UK TR RO PT ZH FR
- **feat** Многоязычные синонимы метаданных (хранятся в `titles:` YAML, рантайм резолвит по языку): Entity (4a), Field/TablePart (4b), Subsystem/Register/InfoReg/Report/Processor (4d-core), длинный хвост (4d-tail)
- **i18n** Переводы 16 ключей (based_on, импорт 1С) на 15 локалей
- **chore** Инструмент `i18ncheck` + git pre-commit hook
- **docs** План 31 — редактор переводов синонимов в конфигураторе (только спецификация, статус «Не начато» — в коде ещё нет)

### 🔐 Роли и права RBAC (PR #9)
- **feat** Управление ролями в конфигураторе + реальное ограничение прав в UI

### 🗄️ SQLite, типы и ref-joins (PR #7, #11)
- **fix** `SQLITE_BUSY` под конкурентной нагрузкой + concurrency-тест
- **feat** Нормализация пути SQLite при добавлении базы
- **fix** Автоопределение кодировки Windows-1251 при загрузке файлов

### 🧮 DSL — развитие языка (План 36, этапы A–E)
- **feat** Этап A — недостающие строковые/датовые/математические функции
- **feat** Этап B и C — `ИнформацияОбОшибке`, поиск по реквизиту, методы объектов
- **feat** Этап D — тип `ТаблицаЗначений` (Колонки.Добавить, Добавить, Итог, Найти, НайтиСтроки, Сортировать, Свернуть, Выгрузить/ЗагрузитьКолонку; итерация `Для Каждого`)
- **feat** Этап E (чтение) — `РегистрыНакопления.X.Движения()/Остатки()/ВыбратьПоРегистратору()` в DSL
- **feat** Мелкие добивки — `Массив.Найти/Установить/ВГраница/Вставить`, файловые операции (`НайтиФайлы`), `Вычислить`
- **feat** Функции `Символ`/`Chr`/`Char` (код → символ)
- **feat** `ЧтениеТекста`/`ЗаписьТекста`/`Файл` + `type:file` для процессоров
- **feat** Модуль менеджера (`X.manager.os`) — `Документы/Справочники.X.Метод()`
- **feat** `Ссылка.ПолучитьОбъект()` возвращает изменяемый объект (#8)
- **fix** Ссылки реквизитов несут менеджера — `ПолучитьОбъект()` работает (#8)
- **fix** `docProxy.LoadObject` обогащает ссылки шапки и ТЧ (#8)
- **fix** `Сред` без длины и присваивание поля структуры через точку
- **fix** nil-операнд в арифметике трактовать как 0, не concat-строку

### ⚙️ Обработки (DataProcessors, План 40)
- **feat** Табличные части в обработках + формы в конфигураторе
- **feat** Типы параметров `text`/`choice`/`reference` + план managed forms
- **feat** Команда `procrun` для офлайн-отладки обработок
- **fix** Шаблон форм обработок — восстановлены потерянные переменные
- **fix** Сворачиваемый модуль обработок + стрелки дерева

### 📋 Ввод на основании (План 38)
- **feat** Ввод на основании — `based_on` + `ОбработкаЗаполнения` + конфигуратор
- **fix** PascalCase-ключи в строках ТЧ при вводе на основании

### 🤖 Инструменты для разработки с ИИ (План 41, итерация 1)
- **feat** `onebase check` — гейт валидации (синтаксис .os, неизвестные функции, YAML-схема) с exit-code для CI/pre-commit
- **feat** `onebase describe --json` — «рентген» конфигурации (read-only контракт)
- **feat** `onebase ai-guide` — справочник, генерируемый из платформы (builtins, схема метаданных)
- **feat** `onebase init` кладёт `AGENTS.md` в новый проект

### 🖥️ UI/UX предприятия и конфигуратора
- **feat** Виджеты главной страницы по подсистемам (План 39)
- **feat** Inline-создание элемента справочника из ссылочного поля + флаг `allow_inline_create`
- **feat** Кнопка-лупа (открыть карточку)
- **feat** Перемещение баз вверх/вниз в списке
- **feat** Оптимистические блокировки документов и справочников
- **feat** Настройка размера страницы списков в `_settings`
- **feat** Расширение ширины форм
- **feat** Явные стрелки сворачивания в дереве конфигуратора

### 🔌 REST API v2
- **refactor** Извлечён `EntityService` для упсёрта + ТЧ + движений + проведения
- **feat** API использует `EntityService` — OnWrite/ТЧ/проведение/versioning
- **test** Интеграционные тесты для нового поведения REST
- **fix** Не терять ТЧ при post/update через REST + сброс всех регистров при отмене проведения

### 🛡️ Безопасность
- **fix** Закрыть unauth-доступ к debug API внутренним токеном

### 🔄 Конвертер из 1С (План 33)
- **fix** Правильный импорт CommonModules/DataProcessors из 1С (closes #13)

### 🔑 Аутентификация
- **feat** Разрешить пустой пароль (kiosk/тестовый режим)

### 🧹 Рефакторинг и инфраструктура
- **refactor** `submit`/`submitEdit` делят общий `parseSubmitForm`
- **refactor** Убрана leaky-abstraction `DB.Pool()`
- **refactor** `buildDSLVars` вынесен в shared-пакет `internal/dslvars`
- **refactor** `Registry.Load` → `LoadOptions` struct вместо 7 позиционных аргументов
- **refactor** `KnownBuiltinNames` собирает имена из фабрик автоматически
- **ci** Авто-добавление diff коммита в dev-сборки релизов + маркер `<!-- summary -->`
- **chore** Убраны упоминания товарного знака «1С» из UI и документации

### 🐛 Прочие багофиксы конфигуратора/лаунчера
- **fix** Кнопки админ-панели не работали из-за локали «O'zbekcha» (PR #11)
- **fix** Кнопка «Пароль» в списке пользователей не работала в WebView2
- **fix** Пустая база ломала JS — клики и меню не работали
- **fix** Диалог выбора папки не открывался на Windows Server 2019
- **fix** Popup-сохранение возвращало `<nil>` вместо подписи
- **fix** Регистронезависимые отборы по кириллице + кнопки ссылок в отчётах (PR #15)
- **fix** Поле `Hierarchical` в `cfgEntity` + сохранение всех полей сущности
- **fix** Восстановлен пропущенный `u.ID` в списке пользователей

---

## 2. Статья для Инфостарт

### OneBase за неделю: управляемые формы, 14 языков интерфейса и инструменты для разработки с ИИ

**Аннотация.** OneBase — открытая бизнес-платформа на Go со знакомым учётным разработчикам DSL: справочники, документы, регистры, отчёты, проведение, запросы. За неделю после релиза v0.3.27 (22–29 мая) в проект влилось 91 изменение и больше 34 тысяч строк кода. Рассказываю, что появилось: от полноценных управляемых форм с двусторонним конвертером до CLI-инструментов, благодаря которым конфигурацию для платформы может писать связка «разработчик + ИИ».

#### Зачем это всё

OneBase я развиваю в свободное время как ответ на простой вопрос: можно ли взять привычную учётному разработчику модель (платформа → конфигурация → информационная база, DSL «как у всех») и реализовать её на современном открытом стеке — Go + PostgreSQL, веб-интерфейс, всё под git и MIT-лицензией. Прошлая статья на Инфостарт описывала фундамент. Эта — про то, как за одну насыщенную неделю платформа догнала по эргономике ещё несколько «взрослых» возможностей.

Пройдусь по главному.

#### 1. Управляемые формы — теперь по-настоящему

Раньше формы в OneBase были упрощёнными: массив видимых полей в YAML и автогенерируемый рендер. Этого хватало для CRUD, но не для реальных прикладных форм с вкладками, командами и обработчиками событий.

За неделю появился полноценный объект метаданных **«управляемая форма»**:

- **декларативное описание** структуры формы в YAML рядом с DSL-модулем (`.form.os`);
- **UI-редактор в конфигураторе** по тому же паттерну, что и редактор печатных форм — split-pane: слева код/структура в Monaco, справа живой предпросмотр;
- **рантайм событий** — `ПриОткрытииФормы`, обработчики команд, объект-обёртка `Объект`, работа с табличными частями (`ТЧ.Добавить`), движения регистров прямо из формы, корректные ISO-даты;
- **двусторонний конвертер** управляемых форм: импорт из XML-формата Enterprise-системы и экспорт обратно. Конвертер построен на нейтральном промежуточном представлении (IR): `Form.xml ↔ IR ↔ .form.yaml`, с сохранением порядка элементов, локализованных заголовков и round-trip-идентичности.

Параллельно управляемые формы получили и **обработки** (DataProcessors, План 40): у обработок появились табличные части, типы параметров (`text`/`choice`/`reference`/`file`) и собственные формы.

Юридически конвертер опирается только на открытые текстовые форматы выгрузки (XML и BSL) и собственные таблицы соответствий — без чужих XSD и без реверс-инжиниринга исполнения.

#### 2. Интерфейс на 14 языках и многоязычные синонимы метаданных

Большой блок (PR #10) — интернационализация. Появилась локализация UI на немецкий, сербский и ещё 12 языков: грузинский, армянский, испанский, казахский, азербайджанский, узбекский, украинский, турецкий, румынский, португальский, китайский, французский.

Но интереснее другое: **синонимы метаданных теперь многоязычные**. Наименование справочника, реквизита, документа, регистра, отчёта, подсистемы можно задать на нескольких языках в блоке `titles:`, и рантайм подставит нужный по языку пользователя (с откатом на дефолтный `title:`, если перевода нет). Пока переводы синонимов вписываются в YAML руками — UI-редактор для них в конфигураторе ещё в планах (План 31, статус «Не начато»).

Чтобы переводы интерфейса не разъезжались, я добавил инструмент `i18ncheck` и повесил его в pre-commit hook — недостающие или лишние ключи в локалях ловятся ещё до коммита.

#### 3. Роли и права (RBAC)

Управление ролями переехало в конфигуратор, и — что важнее — права начали **реально ограничивать интерфейс** (PR #9), а не просто храниться в метаданных. Это первый подход к полноценной ролевой модели.

#### 4. Инструменты для разработки с ИИ

Отдельная история, которой я особенно доволен. Узкое место при работе ИИ-ассистента над конфигурацией — не «умение кодить», а **петля обратной связи**: без headless-способа увидеть структуру и быстро поймать ошибку ИИ работает вслепую.

Итерация 1 (План 41), принцип «CLI-first — команда, текст/JSON, точная ошибка»:

- **`onebase check`** — гейт валидации: синтаксис `.os`, неизвестные функции, YAML-схема всех видов объектов, компиляция запросов — всё с `file:line:col` и ненулевым exit-кодом. Готовый pre-commit/CI-гейт.
- **`onebase describe --json`** — «рентген» конфигурации: стабильный read-only JSON со всеми сущностями, процедурами модулей и списком встроенных функций.
- **`onebase ai-guide`** — справочник по платформе, генерируемый из неё самой (список builtins группируется по источникам автоматически и не устаревает).
- **`onebase init`** теперь кладёт в новый проект `AGENTS.md` — ИИ-ассистент сразу ориентируется в конвенциях репозитория.

Плюс к этому — команда **`onebase procrun`** для офлайн-отладки обработок без поднятия всей базы.

#### 5. DSL стал богаче

Язык дорос ещё на несколько этапов дорожной карты (План 36):

- недостающие строковые, датовые и математические функции;
- `ИнформацияОбОшибке`, поиск по реквизиту, методы объектов;
- функции `Символ`/`Chr`/`Char`, чтение/запись текстовых файлов;
- **модуль менеджера** (`X.manager.os`) — вызовы вида `Документы.X.Метод()`;
- `Ссылка.ПолучитьОбъект()` возвращает изменяемый объект (issue #8) — со всей цепочкой исправлений, чтобы ссылки «несли менеджера» и обогащались данными шапки и табличных частей;
- тип **`ТаблицаЗначений`** (этап D) с полным набором методов: колонки, добавление строк, `Итог`, `Найти`/`НайтиСтроки`, `Сортировать`, `Свернуть`, выгрузка/загрузка колонок, итерация `Для Каждого`;
- чтение **регистров накопления** прямо из DSL (этап E): `РегистрыНакопления.X.Остатки()`, `.Движения()`, `.ВыбратьПоРегистратору()`;
- мелкие добивки: `Массив.Найти/Установить/ВГраница/Вставить`, `НайтиФайлы`, `Вычислить`.

Поправил и пару коварных мелочей: `Сред` без третьего аргумента, присваивание поля структуры через точку, а nil-операнд в арифметике теперь честно трактуется как 0, а не превращает выражение в конкатенацию строк.

#### 6. REST API v2 и единый сервис записи

Я вынес общую логику записи в `EntityService` — единую точку для упсёрта, табличных частей, движений и проведения. Теперь REST API использует тот же сервис, что и UI: одинаковое поведение `OnWrite`, проведение, версионирование. Заодно закрыл неприятный баг — потерю табличных частей при POST/UPDATE через REST и не-сброс регистров при отмене проведения. Покрыл новое поведение интеграционными тестами.

#### 7. SQLite, конкурентность и кодировки

- `SQLITE_BUSY` под конкурентной нагрузкой побеждён (+ concurrency-тест);
- путь к файлу SQLite нормализуется при добавлении базы;
- при загрузке файлов автоматически определяется кодировка Windows-1251 — больше не нужно вручную перекодировать выгрузки.

#### 8. Десятки улучшений эргономики

Виджеты главной страницы по подсистемам, inline-создание элемента справочника прямо из ссылочного поля, кнопка-лупа для открытия карточки, перемещение баз вверх/вниз, оптимистические блокировки документов и справочников, настройка размера страницы списков, явные стрелки сворачивания в дереве конфигуратора. Плюс длинный хвост багофиксов: от «кнопки не работают из-за локали O'zbekcha» и неработающей кнопки «Пароль» в WebView2 до диалога выбора папки на Windows Server 2019 и регистронезависимых отборов по кириллице.

И отдельно — безопасность: закрыл неавторизованный доступ к debug-API внутренним токеном.

#### Итоги недели в цифрах

- **91** изменение, **+34 106 / −2 257** строк;
- **4** влитых PR, закрыты issue **#8** и **#13**;
- крупные блоки: управляемые формы, i18n (14 языков), RBAC, AI-инструменты, REST API v2.

OneBase остаётся проектом выходного дня и открыт на GitHub под MIT. Если тема «открытая платформа с привычным DSL на Go» вам близка — заходите, пробуйте, заводите issue. В следующих итерациях — MCP-сервер поверх `describe`/`check` и продолжение дорожной карты DSL.

*1С, 1С:Предприятие — товарные знаки ООО «1С». OneBase разрабатывается независимо и не аффилирован с ООО «1С».*

---

## 3. План видео (демо-сценарий)

**Хронометраж:** ~10–12 минут. Принцип: от «вау» к «как устроено», каждая фича — короткий живой показ, без слайдов.

### 0. Интро (0:00–0:45)
- Одной фразой: «OneBase — открытая платформа на Go с привычным учётным DSL. За неделю после релиза — 91 изменение, показываю главное».
- На экране: лаунчер со списком баз, запуск демо-базы `trade`.

### 1. Управляемые формы (0:45–3:30) — главный номер, идёт первым
1. Открыть документ в режиме «Предприятие» — показать готовую управляемую форму с вкладками и табличной частью.
2. Добавить строку в ТЧ (`ТЧ.Добавить`), выбрать ссылку через select (не UUID), показать движения регистров.
3. Переключиться в конфигуратор → редактор формы: split-pane Monaco + live preview, поправить элемент → предпросмотр обновился.
4. **Кульминация:** показать двусторонний конвертер — импорт `Form.xml` из 1С → форма в OneBase, и экспорт обратно (round-trip).

### 2. Инструменты для разработки с ИИ (3:30–6:00) — второй по «вау», хорошо заходит аудитории
1. `onebase check` на конфигурации с намеренной ошибкой → точный `file:line:col` и ненулевой exit-code.
2. `onebase describe --json` → показать «рентген» конфигурации (entities, процедуры, builtins).
3. `onebase ai-guide` → сгенерированный из платформы справочник.
4. `onebase init my-app` → показать, что в проект лёг `AGENTS.md`.
5. (Опционально, сильный финал блока) — отдать `describe`/`ai-guide` ИИ-ассистенту и на глазах у зрителя сгенерировать маленький справочник.

### 3. Интерфейс на 14 языках (6:00–7:30)
1. Переключить язык интерфейса (например, на сербский / казахский) — UI перерисовался.
2. Показать **многоязычные синонимы**: задать наименование справочника на двух языках в редакторе переводов → в интерфейсе подставился нужный.
3. Кратко: `i18ncheck` в pre-commit — переводы не разъезжаются.

### 4. Роли и права RBAC (7:30–8:30)
1. В конфигураторе назначить роль с ограничениями.
2. Зайти под пользователем с этой ролью — показать, что интерфейс реально урезан (нет доступа к объекту/команде).

### 5. DSL и REST API (8:30–10:30)
1. DSL: короткий модуль с `Ссылка.ПолучитьОбъект()`, модулем менеджера `Документы.X.Метод()`, новой строковой функцией.
2. `onebase procrun` — офлайн-отладка обработки без поднятия базы.
3. REST API: создать документ с табличной частью через REST → показать, что ТЧ и движения сохранились (тот же `EntityService`, что и UI).

### 6. Эргономика одним проходом (10:30–11:30)
Быстрая нарезка: виджеты главной по подсистемам, inline-создание из ссылочного поля, кнопка-лупа, перемещение баз, оптимистические блокировки. По 5–7 секунд на каждое.

### 7. Финал (11:30–12:00)
- Цифры недели: 91 изменение, +34k строк, 4 PR.
- Призыв: GitHub, MIT, заводите issue. Тизер следующего: MCP-сервер и продолжение DSL-роадмапа.

**Порядок осознанный:** сильнейшие, самые «смотрибельные» фичи (формы и AI-инструменты) — в начале, пока внимание максимально; технические детали (DSL/REST) — в середине; мелкая эргономика — быстрой нарезкой ближе к концу.

---

## 4. Реальные примеры кода (из репозитория)

Все примеры ниже взяты из текущего кода `examples/` и тестов — это рабочие фрагменты, не псевдокод.

### 4.1. Управляемая форма (декларативный YAML)
`examples/trade/forms/пересчёт_цен/объекта.form.yaml` — форма обработки с группой и двумя полями ввода, привязанными к данным через `data_path`:

```yaml
schema: onebase.form/v1
form:
  name: ФормаОбъекта
  kind: object
  entity: ПересчётЦен
  title:
    ru: "Пересчёт цен номенклатуры"

elements:
  - kind: ГруппаФормы
    name: Параметры
    title:
      ru: "Параметры"
    children:
      - kind: ПолеВвода
        name: ПолеПроцент
        title:
          ru: "Процент изменения цены"
        data_path: Объект.Процент
        required: true
        hint: "Положительный — увеличить, отрицательный — уменьшить"

      - kind: ПолеВвода
        name: ПолеГруппа
        title:
          ru: "Группа"
        data_path: Объект.Группа
        hint: "Оставьте пустым для обработки всех товаров"
```

### 4.2. DSL обработки — Запрос, Массив, Структура, Попытка/Исключение
`examples/trade/src/пересчётЦен.proc.os` — модуль обработки, отлаживается офлайн через `onebase procrun`:

```bsl
Процедура Выполнить()
  Процент = Параметры.Процент;
  Если Процент = 0 Тогда
    Error("Процент не может быть нулевым");
  КонецЕсли;

  Запрос = Новый Запрос;
  Запрос.Текст = "ВЫБРАТЬ Наименование, ЦенаПродажи ИЗ Номенклатура";

  Попытка
    Результат = Запрос.Выполнить();
  Исключение
    Сообщить("Ошибка запроса: " + ОписаниеОшибки());
    Возврат;
  КонецПопытки;

  Изменения = Новый Массив;
  Для Каждого Строка Из Результат Цикл
    Если Строка.ЦенаПродажи > 0 Тогда
      Элемент = Новый Структура;
      Элемент.Наименование = Строка.Наименование;
      Элемент.Было = Строка.ЦенаПродажи;
      Элемент.Стало = Строка.ЦенаПродажи * (1 + Процент / 100);
      Изменения.Добавить(Элемент);
    КонецЕсли;
  КонецЦикла;
  // ... вывод результата
КонецПроцедуры
```

### 4.3. Ввод на основании (План 38)
Документ-приёмник объявляет источники в `based_on`, а заполнение пишется в DSL.

`examples/trade/documents/возврат_от_покупателя.yaml`:

```yaml
name: ВозвратОтПокупателя
posting: true
based_on:
  - РеализацияТоваров
numerator:
  prefix: "ВОЗ-"
  length: 5
  period: year
fields:
  - name: Покупатель
    type: reference:Контрагент
  - name: Склад
    type: reference:Склад
tableparts:
  - name: Товары
    fields:
      - name: Номенклатура
        type: reference:Номенклатура
      - name: Количество
        type: number
```

`examples/trade/src/возвратотпокупателя.os` — обработчик заполнения копирует шапку и строки ТЧ из источника:

```bsl
Процедура ОбработкаЗаполнения(ДанныеЗаполнения)
  this.Покупатель = ДанныеЗаполнения.Покупатель;
  this.Склад      = ДанныеЗаполнения.Склад;
  Для Каждого Стр Из ДанныеЗаполнения.Товары Цикл
    Нов = this.Товары.Добавить();
    Нов.Номенклатура = Стр.Номенклатура;
    Нов.Количество   = Стр.Количество;
    Нов.Цена         = Стр.Цена;
    Нов.Сумма        = Стр.Сумма;
  КонецЦикла;
КонецПроцедуры
```

### 4.4. Модуль менеджера — `Документы.X.Метод()` / `Справочники.X.Метод()`
Метод объявляется в `X.manager.os` и вызывается через корневой объект менеджера (поведение зафиксировано в `internal/ui/dsl_documents_manager_test.go`):

```bsl
// Счёт.manager.os
Функция Удвоить(Х)
  Возврат Х * 2;
КонецФункции
```

```bsl
// вызов из любого модуля:
Результат = Документы.Счёт.Удвоить(21);   // → 42
```

### 4.5. `Ссылка.ПолучитьОбъект()` — изменяемый объект (issue #8)
Ссылка теперь «несёт» менеджера, поэтому по ней можно получить изменяемый объект с загруженной шапкой и табличными частями (из `internal/ui/dsl_documents_test.go`):

```bsl
СсылкаДок = Документы.ВозвратОтПокупателя.НайтиПоНомеру("ВП-001");
Объект    = СсылкаДок.ПолучитьОбъект();   // объект с шапкой и ТЧ
Объект.Статус = "Проведён";
Объект.Записать();                         // обновится та же запись по UUID
```

### 4.6. Многоязычные синонимы метаданных
Резолвинг заголовка: `Titles[lang]` побеждает, иначе откат на дефолтный `title:` (поведение из `internal/metadata/types_test.go`). В YAML это выглядит так:

```yaml
name: Контрагенты
title: "Контрагенты компании"     # дефолт / fallback
titles:
  en: "Counterparties"
  sr: "Партнери"
```

При языке `en` интерфейс покажет «Counterparties», при `de` (перевода нет) — «Контрагенты компании».

> ⚠️ Уточнение по факту: править блок `titles:` пока нужно вручную в YAML. UI-редактора переводов синонимов в конфигураторе **нет** — это План 31 со статусом «Не начато»; за неделю по нему добавлена только спецификация.

### 4.7. ТаблицаЗначений (этап D)
Универсальный контейнер с колонками — для агрегации и промежуточных расчётов в DSL (сквозной сценарий из `internal/dsl/interpreter/valuetable_dsl_test.go`):

```bsl
Т = Новый ТаблицаЗначений;
Т.Колонки.Добавить("Товар");
Т.Колонки.Добавить("Сумма");

Стр = Т.Добавить();
Стр.Товар = "Стул";
Стр.Сумма = 100;

Стр2 = Т.Добавить();
Стр2.Товар = "Стол";
Стр2.Сумма = 250;

Возврат Т.Итог("Сумма");   // → 350
```

Поддерживаются `Найти`/`НайтиСтроки(Отбор)`, `Сортировать("Сумма Убыв")`, `Свернуть("Товар", "Сумма")`, `ВыгрузитьКолонку`/`ЗагрузитьКолонку` и итерация `Для Каждого Строка Из Т Цикл`.

---

## 5. Результаты сверки заявлений с кодом

После замечания про «редактор переводов» я перепроверил ключевые фичи по фактическому коду и тестам:

| Заявление в статье | Статус | Чем подтверждается |
|---|---|---|
| AI-инструменты: `check` / `describe` / `ai-guide` / `procrun` | ✅ реализовано | `internal/cli/{check,describe,aiguide,procrun}.go`, все через `rootCmd.AddCommand(...)` |
| RBAC реально ограничивает интерфейс | ✅ реализовано | `Server.can()` → `user.Has(kind,entity,op)`, `requirePerm` отдаёт 403; админ-роуты `/ui/admin/roles`. *Нюанс:* при работе без пользователей (kiosk) доступ открыт — это by design |
| Управляемые формы: рантайм + редактор + конвертер | ✅ реализовано | `handlers_managed_events.go` (`ExecuteEventHandler`), пакет `internal/onec_forms` с `reader_xml`/`writer_xml`, фасад `ImportFromOneC`/`ExportToOneC`/`Validate` |
| Двусторонний конвертер форм (round-trip) | ✅ реализовано | reader+writer для XML и YAML, golden round-trip-тесты в `internal/onec_forms` |
| Многоязычные синонимы метаданных | ✅ реализовано | `Entity.Titles` + резолв по языку (`types_test.go`), хранение в `titles:` YAML |
| Редактор переводов синонимов в конфигураторе | ❌ **не реализовано** | План 31, статус «Не начато»; за неделю только `docs(plans)` |
| ТаблицаЗначений / регистры в DSL / мелкие добивки | ✅ реализовано | `valuetable.go` + тесты, `dsl_registers.go` + тесты, `collections.go`/`builtins_files.go` + тесты |

Все пункты, кроме редактора переводов синонимов, подтверждены реализацией и тестами.
